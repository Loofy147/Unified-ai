
# Unified AI System - Example Configuration File
#
# Copy this file to .env and modify the values as needed.
# These variables control the behavior of the core components.

# -- ModelZoo Settings --
# The file system path where models and their metadata are stored.
MODEL_ZOO_STORAGE_PATH=data/models

# -- ResourceManager Settings --
# The total CPU capacity that the resource manager can allocate.
# Represented as a percentage (e.g., 100.0 for one full core).
RESOURCE_MANAGER_MAX_CPU=100.0

# The total memory capacity in Megabytes (MB) that the resource manager can allocate.
RESOURCE_MANAGER_MAX_MEMORY_MB=4096.0
